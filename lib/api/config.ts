import { DEFAULT_LOCALE } from '@/lib/i18n/config';

// Мова за замовчуванням для core-endpoint'ів виду /core/{language_code}/...
// Узгоджена з дефолтною локаллю інтерфейсу.
export const DEFAULT_LANG: string = DEFAULT_LOCALE;
